import Foundation

extension String {
    /// This functions have been removed because they were super-slow
    /// Our backend always uses `camelCase`
    var pascalCase: String {
        return self
    }
    /// This functions have been removed because they were super-slow
    /// Our backend always uses `camelCase`
    var camelCase: String {
        return self
    }
}
